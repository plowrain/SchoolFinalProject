using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HighestBird : Enemy
{
    protected override void Start()
    {
        //setAbility(_maxHealth: 120, _atk: 11, _atkSpeed: 2f, _def: 5, _speed: 3f, _penetration: 20, _exp: 8, _fixDef: 5, _ele: 3);
        base.Start();


    }

    // Update is called once per frame
    protected override void Update()
    {
        base.Update();
    }
}
